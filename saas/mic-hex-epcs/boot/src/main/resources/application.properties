application.banner.title=Product Catalog
application.banner.code=mic-hex-epcs
application.banner.version=@project.version@

spring.application.name=${application.banner.code}
spring.webflux.base-path=${application.banner.code}

server.port=8200
