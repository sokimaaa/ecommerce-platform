application.banner.title=Shopping Cart
application.banner.code=mic-hex-escs
application.banner.version=@project.version@

spring.application.name=${application.banner.code}
spring.webflux.base-path=${application.banner.code}

server.port=8400

grpc.client.eoms.holder.host=localhost
grpc.client.eoms.holder.port=8110
grpc.client.eoms.holder.connection-timeout=1000
grpc.client.eoms.holder.deadline=1000
grpc.client.eoms.holder.idle-timeout=1000
